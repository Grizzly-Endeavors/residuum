# Design

Designs for work that is not built yet.

A document lives here while its work is pending. Once the work ships, `../systems-usage/` describes the resulting behavior and the design document moves to `../archive/` — a design doc is never the record of how the system currently works.

State the status at the top of each document, so a reader knows whether it is still live.
