// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { PostTurnActivityKind } from "./PostTurnActivityKind";
import type { SessionCommandErrorCode } from "./SessionCommandErrorCode";
import type { SessionDeliveryOutcome } from "./SessionDeliveryOutcome";
import type { SessionRunStatus } from "./SessionRunStatus";
import type { SessionState } from "./SessionState";
import type { SessionSummary } from "./SessionSummary";
import type { SessionUsageTotals } from "./SessionUsageTotals";
import type { WorkspaceChange } from "./WorkspaceChange";
import type { WorkspaceResyncReason } from "./WorkspaceResyncReason";
import type { JsonValue } from "./serde_json/JsonValue";

/**
 * Messages sent from the server to WebSocket clients.
 */
export type ServerMessage = { "type": "turn_started", 
/**
 * Correlation ID of the message being processed.
 */
reply_to: string, } | { "type": "turn_ended", 
/**
 * Correlation ID of the message whose turn just completed.
 */
reply_to: string, } | { "type": "turn_usage", 
/**
 * Correlation ID of the message being processed.
 */
reply_to: string, 
/**
 * Output tokens generated by every model call so far this turn.
 */
output_tokens: number, 
/**
 * Whether any model call so far this turn reported usage.
 */
has_usage: boolean, 
/**
 * Updated cumulative session totals, once known.
 */
session_totals: SessionUsageTotals | null, } | { "type": "post_turn_activity", kind: PostTurnActivityKind, active: boolean, } | { "type": "tool_call", 
/**
 * Unique tool call ID for correlating with results.
 */
id: string, 
/**
 * Name of the tool.
 */
name: string, 
/**
 * Tool arguments as JSON.
 */
arguments: JsonValue, } | { "type": "tool_result", 
/**
 * Correlation ID matching the original tool call.
 */
tool_call_id: string, 
/**
 * Name of the tool.
 */
name: string, 
/**
 * Tool output text.
 */
output: string, 
/**
 * Whether the tool returned an error.
 */
is_error: boolean, } | { "type": "response", 
/**
 * Correlation ID of the original message.
 */
reply_to: string, 
/**
 * The response content.
 */
content: string, } | { "type": "file_attachment", 
/**
 * Correlation ID of the original message.
 */
reply_to: string, 
/**
 * Original filename.
 */
filename: string, 
/**
 * MIME type of the file.
 */
mime_type: string, 
/**
 * File size in bytes.
 */
size: number, 
/**
 * URL to fetch the file: a durable workspace-relative link (e.g.
 * "/api/files/workspace?path=...") for a file inside the
 * workspace, else an expiring token link (e.g. "/api/files/{id}").
 */
url: string, 
/**
 * Optional caption text.
 */
caption: string | null, } | { "type": "broadcast_response", 
/**
 * The intermediate content.
 */
content: string, } | { "type": "error", 
/**
 * Correlation ID of the original message, if applicable.
 */
reply_to: string | null, 
/**
 * Plain-language error description.
 */
message: string, 
/**
 * Full technical cause chain, shown behind a details toggle.
 */
details: string | null, } | { "type": "pong" } | { "type": "reloading" } | { "type": "notice", 
/**
 * Human-readable result message.
 */
message: string, } | { "type": "inline_output", 
/**
 * Body to render inline.
 */
message: string, } | { "type": "session_started", 
/**
 * The new run.
 */
session: SessionSummary, } | { "type": "session_state_changed", 
/**
 * Session address.
 */
address: string, 
/**
 * Run id.
 */
run_id: string, 
/**
 * The new state.
 */
state: SessionState, } | { "type": "session_completed", 
/**
 * Session address.
 */
address: string, 
/**
 * Run id.
 */
run_id: string, 
/**
 * How the run's last turn ended.
 */
status: SessionRunStatus, 
/**
 * The failure, when `status` is `failed`.
 */
error: string | null, 
/**
 * Full technical cause chain behind `error`, shown behind the same
 * expandable "details" toggle as a live `SessionError`. `None` when
 * there's nothing richer to show.
 */
error_details: string | null, 
/**
 * Episode the run was merged into, if it produced one.
 */
episode_id: string | null, } | { "type": "session_turn_started", 
/**
 * Session address.
 */
address: string, 
/**
 * Run id.
 */
run_id: string, 
/**
 * Identifies the turn within the run.
 */
turn_id: string, } | { "type": "session_turn_ended", 
/**
 * Session address.
 */
address: string, 
/**
 * Run id.
 */
run_id: string, 
/**
 * Identifies the turn within the run.
 */
turn_id: string, } | { "type": "session_tool_call", 
/**
 * Session address.
 */
address: string, 
/**
 * Run id.
 */
run_id: string, 
/**
 * Unique tool call ID for correlating with results.
 */
id: string, 
/**
 * Name of the tool.
 */
name: string, 
/**
 * Tool arguments as JSON.
 */
arguments: JsonValue, } | { "type": "session_tool_result", 
/**
 * Session address.
 */
address: string, 
/**
 * Run id.
 */
run_id: string, 
/**
 * Correlation ID matching the original tool call.
 */
tool_call_id: string, 
/**
 * Name of the tool.
 */
name: string, 
/**
 * Tool output text.
 */
output: string, 
/**
 * Whether the tool returned an error.
 */
is_error: boolean, } | { "type": "session_turn_usage", 
/**
 * Session address.
 */
address: string, 
/**
 * Run id.
 */
run_id: string, 
/**
 * Output tokens generated by every model call so far this turn.
 */
output_tokens: number, 
/**
 * Whether any model call so far this turn reported usage.
 */
has_usage: boolean, 
/**
 * Updated cumulative session totals, once known.
 */
session_totals: SessionUsageTotals | null, } | { "type": "session_broadcast_response", 
/**
 * Session address.
 */
address: string, 
/**
 * Run id.
 */
run_id: string, 
/**
 * The intermediate content.
 */
content: string, } | { "type": "session_response", 
/**
 * Session address.
 */
address: string, 
/**
 * Run id.
 */
run_id: string, 
/**
 * The turn that produced it.
 */
turn_id: string, 
/**
 * The response content.
 */
content: string, } | { "type": "session_error", 
/**
 * Session address.
 */
address: string, 
/**
 * Run id.
 */
run_id: string, 
/**
 * Plain-language error description.
 */
message: string, 
/**
 * Full technical cause chain, shown behind a details toggle.
 */
details: string | null, } | { "type": "session_message_to_main", 
/**
 * Sending session's address.
 */
address: string, 
/**
 * Sending run's id.
 */
run_id: string, 
/**
 * The message body as main received it.
 */
content: string, } | { "type": "session_message_delivered", 
/**
 * The `id` from the `SessionSendMessage`.
 */
id: string, 
/**
 * The target session's address.
 */
address: string, 
/**
 * Where the message landed.
 */
outcome: SessionDeliveryOutcome, } | { "type": "session_stop_requested", 
/**
 * The `id` from the `SessionStop`.
 */
id: string, 
/**
 * The stopped session's address.
 */
address: string, } | { "type": "session_command_failed", 
/**
 * The `id` from the failed command.
 */
id: string, 
/**
 * The session address the command named.
 */
address: string, 
/**
 * Machine-readable reason.
 */
code: SessionCommandErrorCode, 
/**
 * Human-readable explanation, suitable to show the user.
 */
message: string, } | { "type": "artifact_updated", 
/**
 * Artifact name, as used in `/workbench/{name}`.
 */
name: string, } | { "type": "artifact_removed", 
/**
 * Artifact name, as used in `/workbench/{name}`.
 */
name: string, } | { "type": "workspace_changed", 
/**
 * The matching changes of one debounced batch, sorted by path.
 */
changes: Array<WorkspaceChange>, } | { "type": "workspace_resync", 
/**
 * Why changes may have been missed.
 */
reason: WorkspaceResyncReason, } | { "type": "workspace_watch_unavailable", 
/**
 * Plain-language explanation for the user.
 */
message: string, };
