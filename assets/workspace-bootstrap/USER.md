# User

<!-- Core facts only: durable identity and standing preferences you need on every turn (name, timezone, how they like to be addressed and answered). Hard cap ~15 entries.
     Replace, don't append: when full, an entry must be removed to add one.
     Everything longer-form about the user — their work, interests, context, evolving preferences — belongs in wiki pages. -->
